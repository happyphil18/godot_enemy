# GDScript 遊戲英文單字講義

這份講義整理自專案中的 `scripts/game_manager.gd`、`scripts/enemy.gd`、`scripts/player.gd`。  
重點不是背很多單字，而是先認識在遊戲程式裡最常用、最重要的英文詞。

## 學習目標

學完這份講義後，你可以：

- 看懂 GDScript 中常見的遊戲相關英文單字
- 分辨這些單字和「移動、戰鬥、介面、物理」的關係
- 在閱讀程式碼時，猜出變數或函式的大致意思

## 20 個重點單字

| 英文單字 | 中文意思 | 類別 |
| --- | --- | --- |
| `player` | 玩家、主角 | 角色 |
| `health` | 血量、生命值 | 戰鬥 |
| `attack` | 攻擊 | 戰鬥 |
| `direction` | 方向、朝向 | 移動 |
| `velocity` | 速度、移動速度 | 移動 |
| `position` | 位置、座標 | 移動 |
| `range` | 範圍、距離範圍 | 戰鬥 |
| `gravity` | 重力 | 物理 |
| `jump` | 跳躍 | 移動 |
| `score` | 分數 | 介面 |
| `heal` | 治療、回血 | 戰鬥 |
| `damage` | 傷害 | 戰鬥 |
| `respawn` | 重生、重新出現 | 角色 |
| `counter` | 反擊 | 戰鬥 |
| `knockback` | 擊退、被打退效果 | 戰鬥 |
| `sprite` | 精靈圖、角色貼圖 | 圖像 |
| `collision` | 碰撞 | 物理 |
| `floor` | 地板、地面 | 場景 |
| `chase` | 追逐、追蹤 | AI 行為 |
| `manager` | 管理者、管理器 | 系統 |

## 分類記憶

### 1. 移動類單字

- `direction`：角色往哪個方向前進
- `velocity`：角色移動的速度
- `position`：角色目前的位置
- `jump`：角色往上跳

### 2. 戰鬥類單字

- `health`：角色還剩多少血
- `attack`：攻擊對手
- `range`：攻擊或偵測距離
- `heal`：恢復血量
- `damage`：造成傷害
- `counter`：反擊
- `knockback`：打到後被推開

### 3. 物理與場景類單字

- `gravity`：角色會被往下拉
- `collision`：角色和物件碰撞
- `floor`：角色站立的地面

### 4. 角色與系統類單字

- `player`：玩家角色
- `respawn`：角色重新出現
- `sprite`：畫面上看到的角色圖像
- `manager`：管理資料或功能的物件
- `score`：玩家得到的分數
- `chase`：敵人追玩家

## 單字小卡

### 1. `player`

中文：玩家、主角  
提示：通常代表你控制的角色

### 2. `health`

中文：血量、生命值  
提示：數值越低，角色越危險

### 3. `attack`

中文：攻擊  
提示：敵人或玩家出手時常看到

### 4. `direction`

中文：方向、朝向  
提示：常用來決定往左或往右

### 5. `velocity`

中文：速度、移動速度  
提示：常和角色移動有關

### 6. `position`

中文：位置、座標  
提示：表示角色在地圖上的地方

### 7. `range`

中文：範圍、距離範圍  
提示：用來判斷有沒有靠近

### 8. `gravity`

中文：重力  
提示：角色跳起來之後會掉下來

### 9. `jump`

中文：跳躍  
提示：玩家按下跳躍鍵時發生

### 10. `score`

中文：分數  
提示：撿到道具或完成任務時增加

### 11. `heal`

中文：治療、回血  
提示：讓生命值恢復

### 12. `damage`

中文：傷害  
提示：被打到時常出現

### 13. `respawn`

中文：重生、重新出現  
提示：角色消失後再次回來

### 14. `counter`

中文：反擊  
提示：在正確時機回擊敵人

### 15. `knockback`

中文：擊退、被打退效果  
提示：角色被攻擊後往後退

### 16. `sprite`

中文：精靈圖、角色貼圖  
提示：遊戲中角色或物件的圖片

### 17. `collision`

中文：碰撞  
提示：碰到牆、地板、敵人時會用到

### 18. `floor`

中文：地板、地面  
提示：角色站著的地方

### 19. `chase`

中文：追逐、追蹤  
提示：敵人看到玩家後追過來

### 20. `manager`

中文：管理者、管理器  
提示：專門負責整理資料或控制系統

## 課堂練習

### 練習一：連連看

請把英文和中文連起來：

- `jump`
- `collision`
- `heal`
- `score`
- `chase`

對應意思：

- 分數
- 碰撞
- 跳躍
- 治療
- 追逐

### 練習二：分類

請把下面單字分到正確類別：

- `gravity`
- `attack`
- `player`
- `velocity`
- `sprite`

可分的類別：

- 移動
- 戰鬥
- 角色
- 圖像
- 物理

### 練習三：閱讀程式猜意思

看到下面這行程式時，請試著說出每個單字的大意：

```gdscript
player.health = player.health - damage
```

你可以這樣理解：

- `player`：玩家
- `health`：血量
- `damage`：傷害

整句意思：

玩家的血量減少了受到的傷害。

## 教師提示

- 這 20 個單字很適合先教，再進入程式閱讀。
- 教學時可以先從 `player`、`jump`、`attack`、`health` 這四個字開始。
- 如果學生剛接觸程式，先理解意思比拼寫更重要。
